---

- name: Copy binary
  copy:
    src: "{{BINARY}}"
    dest: /usr/bin/anekad
    mode: 0755
  notify: restart anekad

- name: Copy new genesis.json file, if available
  when: "GENESISFILE is defined and GENESISFILE != ''" 
  copy:
    src: "{{GENESISFILE}}"
    dest: /home/anekad/.anekad/config/genesis.json
  notify: restart anekad

- name: Download genesis.json URL, if available
  when: "GENESISURL is defined and GENESISURL != ''"
  get_url:
    url: "{{GENESISURL}}"
    dest: /home/anekad/.anekad/config/genesis.json
    force: yes
  notify: restart anekad

- name: Reset network
  when: UNSAFE_RESET_ALL | default(false) | bool
  command: "sudo -u anekad anekad unsafe-reset-all"
  notify: restart anekad

